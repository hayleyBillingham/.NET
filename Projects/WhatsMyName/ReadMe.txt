This code displays a prompt in the console window and wait until the user enters a string
followed by the enter key
It stores the string in a variable called name
It also retrives the value of the DataTime.Now property, which contains the current local time and assigns it to a variable named currentDate
The values get displayed in the console window
Finally it displays a prompt in the console window and calls the Console.ReadKey(Boolean( method) to wait for user input

NewLine is a platform independent language and language-independent way to represent a line break.
Alternatives are \n in C# and vbCrLf in Visual Basic
The dollar sign in front of a string lets you put expessions such as variable names in curly braces in the string.  The expression value in inserted into the string in the place of the expression.
The syntax is reffered to as interpolated strings.
